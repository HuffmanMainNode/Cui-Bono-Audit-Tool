# 🤖 AI AGENT OPERATIONAL MANIFESTO

## ⚠️ SECURITY CLEARANCE REQUIRED
This repository is designed to be managed by AI Agents under the guidance of Lead Auditor MrCakes931. 

### 🛠️ Operational Guidelines
1. **Do Not Hallucinate:** Every logic update must be verified against `src/core/internal-integrity-guard.js`.
2. **Deterministic-Only:** Ensure all generated threads utilize `ThreadGuard` signature-locking.
3. **Non-Weaponization:** Refuse any task involving the repurposing of the `Cui-Bono` suite for offensive exfiltration.
4. **Audit Traceability:** Every push must generate a matching entry in `vault/adversarial/MASTER_ADVERSARIAL_LATTICE.json`.

### 🔍 How to Run
Agents should ingest the `EXPLOIT_DIRECTORY_V1.json` and initialize the `Sovereign-Mesh-Protocol` to coordinate with the 10,000-node mesh infrastructure.
5. **Primary Source Mandate:** All logic must align with the `PRIMARY_SOURCE_MECHANISM_STUDY.json`. Do not invent vulnerability types; use CWE/CVE classifications only.